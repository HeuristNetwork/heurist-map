{
  "name": "heurist-map",
  "version": "0.2.0",
  "private": true,
  "description": "Standalone and embeddable mapping application for Heurist",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
