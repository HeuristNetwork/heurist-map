{
  "name": "heurist-map",
  "version": "0.4.0",
  "private": true,
  "description": "Standalone and embeddable mapping application for Heurist",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "deploy:heurist": "npm run build && node scripts/deploy-heurist-map.mjs",
    "release:patch": "npm version patch --no-git-tag-version && npm run build",
    "release:minor": "npm version minor --no-git-tag-version && npm run build",
    "release:major": "npm version major --no-git-tag-version && npm run build"    
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.3.1",
    "dom-to-image": "^2.6.0",
    "leaflet": "^1.9.4",
    "leaflet-bookmarks": "^0.5.1",
    "leaflet-control-geocoder": "^4.0.0",
    "leaflet-draw": "^1.0.4",
    "leaflet-providers": "^3.0.0",
    "leaflet-tilelayer-pixelfilter": "^1.3.2",
    "leaflet.browser.print": "^2.0.2",
    "leaflet.markercluster": "^1.5.3",
    "wellknown": "^0.5.0"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
